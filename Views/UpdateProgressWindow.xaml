<Window x:Class="MelodyPaieRDC.Views.UpdateProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Mise à jour — Melody Paie RDC"
        Height="220" Width="480"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Topmost="True"
        ShowInTaskbar="True"
        WindowStyle="SingleBorderWindow"
        Background="#F5F7FA"
        FontFamily="{materialDesign:MaterialDesignFont}">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <materialDesign:PackIcon Kind="Download" Width="28" Height="28"
                                     Foreground="{DynamicResource BrandPrimaryBrush}"
                                     Margin="0,0,12,0" VerticalAlignment="Center"/>
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Mise à jour en cours" FontSize="17" FontWeight="SemiBold" Foreground="#0F172A"/>
                <TextBlock Text="Ne fermez pas cette fenêtre." FontSize="11.5" Foreground="#64748B" Margin="0,4,0,0"/>
            </StackPanel>
        </StackPanel>

        <TextBlock Grid.Row="1"
                   Text="{Binding Statut}"
                   TextWrapping="Wrap"
                   FontSize="12.5"
                   Foreground="#475569"
                   Margin="0,0,0,14"/>

        <ProgressBar Grid.Row="2"
                     Value="{Binding Progression, Mode=OneWay}"
                     Maximum="100"
                     Height="10"
                     Margin="0,0,0,10"/>

        <TextBlock Grid.Row="3"
                   Text="{Binding Progression, StringFormat={}{0:0}%}"
                   HorizontalAlignment="Right"
                   FontSize="12"
                   FontWeight="SemiBold"
                   Foreground="{DynamicResource BrandPrimaryBrush}"/>
    </Grid>
</Window>
