<Window x:Class="MelodyPaieRDC.Views.AbsencesCongesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Absences et congés"
        Height="500" Width="680"
        WindowStartupLocation="CenterOwner"
        Background="#F5F5F5"
        TextElement.Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
        FontFamily="{materialDesign:MaterialDesignFont}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock FontSize="18" FontWeight="SemiBold" Margin="0,0,0,4">
                <Run Text="Absences et congés – "/>
                <Run Text="{Binding NomEmploye}" FontWeight="Bold"/>
            </TextBlock>
            <TextBlock Text="Les absences « non payées » réduisent le salaire proportionnellement au nombre de jours concernés dans la période de paie. Les congés payés restent pris en compte sans perte de salaire." 
                       FontSize="12" Foreground="Gray" Margin="0,0,0,12"/>
        </StackPanel>

        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="12" Margin="0,0,0,12">
            <Border.Effect>
                <DropShadowEffect BlurRadius="6" ShadowDepth="2" Opacity="0.1"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="Nouvelle absence / congé" FontWeight="SemiBold" Margin="0,0,0,8"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,0,8">
                        <TextBlock Text="Type" Margin="0,0,0,2"/>
                        <ComboBox ItemsSource="{Binding TypesAbsence}" SelectedItem="{Binding Type, Mode=TwoWay}" IsEditable="True"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2" Margin="0,0,0,8">
                        <TextBlock Text="Date début" Margin="0,0,0,2"/>
                        <DatePicker SelectedDate="{Binding DateDebut, Mode=TwoWay}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="4" Margin="0,0,0,8">
                        <TextBlock Text="Date fin" Margin="0,0,0,2"/>
                        <DatePicker SelectedDate="{Binding DateFin, Mode=TwoWay}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="6" Margin="0,0,0,8" VerticalAlignment="Bottom">
                        <CheckBox Content="Rémunérée (congé payé)" IsChecked="{Binding EstPaye, Mode=TwoWay}" Margin="0,0,0,8"/>
                    </StackPanel>
                </Grid>
                <Button Content="Ajouter l'absence" Command="{Binding AjouterCommand}"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        materialDesign:ButtonAssist.CornerRadius="6" Padding="12,8" HorizontalAlignment="Left" Margin="0,8,0,0"/>
            </StackPanel>
        </Border>

        <DataGrid Grid.Row="2" ItemsSource="{Binding AbsencesConges}" SelectedItem="{Binding Selectionne, Mode=TwoWay}"
                  AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False"
                  HeadersVisibility="Column" Margin="0,0,0,12" Background="White">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="100"/>
                <DataGridTextColumn Header="Début" Binding="{Binding DateDebut, StringFormat=d}" Width="95"/>
                <DataGridTextColumn Header="Fin" Binding="{Binding DateFin, StringFormat=d}" Width="95"/>
                <DataGridTextColumn Header="Payée" Binding="{Binding EstPaye}" Width="60"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Supprimer l'absence sélectionnée" Command="{Binding SupprimerCommand}"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    materialDesign:ButtonAssist.CornerRadius="6" Padding="12,8"/>
        </StackPanel>
    </Grid>
</Window>
