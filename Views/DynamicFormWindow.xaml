<Window x:Class="MelodyPaieRDC.Views.DynamicFormWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:MelodyPaieRDC.Views"
        Title="{Binding TitreFenetre}"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        MinWidth="400" MinHeight="300">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="{Binding Definition.Title}" FontSize="16" FontWeight="SemiBold"/>
            <TextBlock x:Name="DescriptionBlock" FontSize="12" Foreground="#607D8B"
                       TextWrapping="Wrap" Margin="0,4,0,0"/>
        </StackPanel>

        <local:DynamicFormControl x:Name="FormControl" Grid.Row="1"/>
    </Grid>
</Window>
